<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.islamicmerriageusa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IslamicMerriageUSA">
        <activity android:name=".ReviewActivity"></activity>
        <activity android:name=".OnBoardActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".RessetPassActivity" />
        <activity android:name=".ForgetPassActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".ChengePassActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>